# dependencies
node_modules/

# env secrets — MUST never be committed
.env.local
.env*.local

# next.js build
.next/
out/
*.tsbuildinfo

# os / editor noise
.DS_Store

# local history (vs code extension) — keep out of git
.history/

# workbuddy local project memory (tooling, not part of the app)
.workbuddy/
